package main.java;

public class comments {

	


	/*
	 * // the path of your tess data folder inside the extracted file String text =
	 * tesseract.doOCR(new File(urls.filePath + "/KushOCR.jpg"));
	 * //System.out.print(text); String reciptNumber = text.substring(17, 23);
	 * 
	 * System.out.println(reciptNumber);
	 */
	
	
	/*
	 * for(String allReciept : receiptNumberList) { System.out.println(allReciept);
	 * }
	 */
}
